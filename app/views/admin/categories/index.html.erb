<h3>카테고리 관리</h3>
		<p>카테고리를 관리합니다.</p>
		<p></p>
		<div class="row">
			<div class="span6">
				<table class="table">
					<thead>
						<tr>
							<td class="span5">카테고리 이름</td>
							<td class="span1 align-right"></td>
						</tr>	
					</thead>
					<tbody>
						<%# binding.pry %>
						<% if @categories.length != 0 %> 
							<% @categories.each do |category| %>
								<tr>
									<td class="span5"><%= link_to category.title, edit_admin_category_path(category) %></td>
									<td class="span1 align-right">
										<%= link_to "삭제", admin_category_path(category), :method => 'delete', data: { :confirm => '정말로 삭제하시겠습니까?' }, :class => 'text-error'%>
									</td>
								</tr>	
							<% end %>
						<% else %>
							<tr>
									<td colspan="2">
										카테고리가 없습니다.
									</td>
								</tr>
						<% end %>
					</tbody>
				</table>
			</div>
			<div class="span6">
				<h4>새 카테고리</h4>
				<p>카테고리를 추가합니다.</p>
				<hr>

				<%= form_for [:admin, @category] do |f| %>
				<div class="row">
					<div class="span6 flash-container">
						<% if @category.errors.messages.length > 0 %>
						<ul class="alert alert-error unstyled">
							<% @category.errors.each do |key, value| %>
								<li><%= value %></li>
							<% end %>
						</ul>
						<% end %>
					</div>
					<div class="span6">
						<%= f.label :title, "카테고리 이름" %>
						<%= f.text_field :title %>
					</div>
					<hr>
					<div class="span6">
						<%= f.submit "추가" , :class => 'btn btn-primary'%>
					</div>
				</div>
				<% end %>
			</div>

		</div>