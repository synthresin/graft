<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "gra-ft admin" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->
    <%= javascript_include_tag "admin" %>
    <%= stylesheet_link_tag "admin", :media => "all" %>
  </head>
  <body>

    <div class="navbar navbar-static-top navbar-inverse">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to 'gra-ft', '#', class: 'brand' %>
          <div class="container nav-collapse">
            <% if admin_signed_in? %>
            <ul class="nav">
              <li class="divider-vertical"></li>
              <li>
                <%= link_to '#' do %>
                  <i class="icon-facetime-video"></i>&nbsp;&nbsp;works
                <% end %>
              </li>
              <li class="divider-vertical"></li>
            </ul>
            <%= link_to '로그아웃', destroy_admin_session_path, :method => 'delete', :class => 'pull-right btn btn-primary' %>
            <% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>
    <div class="container">
        <div class="row">
          <div class="span12">
            <br/>
            <%= bootstrap_flash %>
            <%= yield %>
          </div>
        </div><!--/row-->
    </div> <!-- /container -->
    <div class="container" style="margin-top:20px;">
      <%= debug(params) if Rails.env.development? %>
    </div>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->

    

  </body>
</html>
